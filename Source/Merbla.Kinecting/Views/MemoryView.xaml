<UserControl x:Class="Merbla.Kinecting.Views.MemoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:jas="clr-namespace:Merbla.Kinecting.WPF.Controls;assembly=Merbla.Kinecting.WPF.Controls"
             xmlns:local="clr-namespace:Merbla.Kinecting.WPF.Controls.Controls;assembly=Merbla.Kinecting.WPF.Controls">
  <UserControl.Resources>
        <Style x:Key="MemoryButtonStyle" TargetType="{x:Type Button}" >
            <Setter Property="Height" Value="100"></Setter>
            <Setter Property="Width" Value="100"></Setter>
        </Style>
  </UserControl.Resources>
  <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Style="{StaticResource MemoryButtonStyle}" />
            <Button Style="{StaticResource MemoryButtonStyle}" />
            <Button Style="{StaticResource MemoryButtonStyle}" />
            <Button Style="{StaticResource MemoryButtonStyle}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Style="{StaticResource MemoryButtonStyle}" />
            <Button Style="{StaticResource MemoryButtonStyle}" />
            <Button Style="{StaticResource MemoryButtonStyle}" />
            <Button Style="{StaticResource MemoryButtonStyle}" />
        </StackPanel>

         
    </StackPanel>
</UserControl>
